# Leave unset for local dev (falls back to http://localhost:5000/api).
# In Vercel, set this to your deployed Render backend URL, e.g.:
# REACT_APP_API_URL=https://schedula-xxxx.onrender.com/api
REACT_APP_API_URL=
